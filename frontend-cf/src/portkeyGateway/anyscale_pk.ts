import Portkey from 'portkey-ai'
import 
 
default export const portkey = new Portkey({
    apiKey: "PORTKEY_API_KEY", // defaults to process.env["PORTKEY_API_KEY"]
    virtualKey: "ANYSCALE_VIRTUAL_KEY", // Your Anyscale Virtual Key
    config:
})